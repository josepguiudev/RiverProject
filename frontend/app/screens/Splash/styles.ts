import { ViewStyle, TextStyle } from 'react-native';
// Importamos la utilidad que creamos en app/utils/device.ts


export type SplashPageStyles = {
  container: ViewStyle;
  title: TextStyle;
  subtitle: TextStyle;
}

