import { View } from "react-native";
import globalStyles from "@/assets/globalStyles/globalStyles";

export default function HomeScreen({ navigation }: any) {
    return (
        <View style={globalStyles.padre}>

        </View>
    );
}
